<!DOCTYPE html>
<html>
<head>
  <title>Address Book - List Contacts</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
</head>
<body>
  <div class="container">
    <h1>A List of All Your Contacts :)</h1>
    <ul>
      @foreach (var contact in Model)
      {
        <li>@contact.GetName()</a></li>
        <ul>
          <li>@contact.GetPhone()</li>
          <li>@contact.GetEmail()</li>
          <h3>Address</h3>
          <ul>
            <li>@contact.GetAddress().GetStreet()</li>
            <li>@contact.GetAddress().GetCity()</li>
            <li>@contact.GetAddress().GetState()</li>
            <li>@contact.GetAddress().GetZip()</li>
          </ul>
        </ul>
      }
    </ul>
    <a href="/create_contact">Create a New Contact</a>
    <a href="/contacts_deleted">Delete All Contacts</a>
  </div>
</body>
</html>